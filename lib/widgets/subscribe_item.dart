import 'package:flutter/material.dart';

class SubscribeItem extends StatelessWidget {
  const SubscribeItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
